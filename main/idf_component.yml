## IDF Component Manager Manifest File
dependencies:
  espressif/mdns:
    version: "^1.0.3"
    rules:
      - if: "idf_version >=5.0"
  espressif/cjson:
    version: "^1.7.0"
    rules:
      - if: "idf_version >=6.0"
  espressif/mqtt:
    version: "^1.0.0"
    rules:
      - if: "idf_version >=6.0"
  Molorius/esp32-websocket:
    git: https://github.com/nopnop2002/esp32-websocket
